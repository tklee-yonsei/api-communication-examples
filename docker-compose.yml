# 마이크로서비스 API 통신 테스트 환경
#
# 사용법:
#   개발:     docker compose --profile dev up -d
#   프로덕션: docker compose --profile prod up -d

services:
  # ===========================================
  # FastAPI REST API 서버 (개발용)
  # 볼륨 마운트 + 핫리로드 활성화
  # ===========================================
  rest-api-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: rest_api/Dockerfile.dev
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./communication:/app/communication
      - ./rest_api:/app/rest_api
    networks:
      - app-network

  # ===========================================
  # FastAPI REST API 서버 (프로덕션용)
  # 이미지 빌드 시 코드 복사, 볼륨 마운트 없음
  # ===========================================
  rest-api-prod:
    profiles: ["prod"]
    build:
      context: .
      dockerfile: rest_api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - app-network

  # ===========================================
  # REST UI (개발용)
  # 볼륨 마운트로 실시간 코드 반영
  # ===========================================
  communication-ui-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: communication_ui/Dockerfile.dev
    ports:
      - "3001:3000"
    environment:
      - REST_API_BASE_URL=http://rest-api-dev:8080
    volumes:
      - ./communication_ui:/app/communication_ui
      - ./communication:/app/communication
      - ./rest_api:/app/rest_api
    depends_on:
      rest-api-dev:
        condition: service_started
    networks:
      - app-network

  # ===========================================
  # REST UI (프로덕션용)
  # 이미지 빌드 시 코드 복사, 볼륨 마운트 없음
  # ===========================================
  communication-ui-prod:
    profiles: ["prod"]
    build:
      context: .
      dockerfile: communication_ui/Dockerfile
    ports:
      - "3001:3000"
    environment:
      - REST_API_BASE_URL=http://rest-api-prod:8080
    depends_on:
      rest-api-prod:
        condition: service_started
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
